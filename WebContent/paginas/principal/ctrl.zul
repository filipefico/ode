<?init class="ode._infraestruturaBase.util.AuthInit"?>
<zk>
<zscript>	
	Map parametros = Executions.getCurrent().getArg();
	String use = (String) parametros.get("use");
</zscript>
	<window use="${use}" />
</zk>
